# local test

```bash
# install dependencies
npm install

# generate a local test data
node ./generate_test_data.js

# start a local node
hh node

# open another terminal, run the test
run ./index.js --network localhost
```
